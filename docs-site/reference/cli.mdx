---
title: "NiceEval CLI: commands, flags, and exit codes reference"
sidebarTitle: "CLI"
description: "NiceEval CLI reference for exp, init, list, clean, and view, plus experiment selection, eval filtering, sandbox, concurrency, budget, and JUnit CI output."
---

The [NiceEval](https://niceeval.com/) CLI is the entry point for discovering, running, and viewing evals. Actual eval execution follows an experiment-first model: **`exp` selects a commit-ready run configuration**, and only the positional arguments after the experiment filter eval IDs by prefix. Agent, model, and flags live in `experiments/`, not in ad hoc CLI arguments.

## Commands

<CardGroup cols={2}>
  <Card title="npx niceeval exp &lt;group&gt; [id-prefix]" icon="flask">
    Run a named experiment group or config. Optional trailing positional args filter by eval ID prefix.
  </Card>
  <Card title="npx niceeval init" icon="wand-magic-sparkles">
    Create an empty `evals/` directory and a minimal `niceeval.config.ts`; no sample eval file is generated. Also writes/refreshes a niceeval guidance block — into `AGENTS.md` if the project already has one, into `CLAUDE.md` if only that exists, or creates a new `AGENTS.md` if neither exists (see [Agent feedback loop](/guides/agent-feedback-loop)).
  </Card>
  <Card title="npx niceeval list" icon="list">
    Discover and print all evals without running them.
  </Card>
  <Card title="npx niceeval clean" icon="trash">
    Remove historical snapshot artifacts under `.niceeval/`.
  </Card>
  <Card title="npx niceeval view" icon="eye">
    Open the result viewer for the most recent run's artifacts.
  </Card>
</CardGroup>

## Output language

The [NiceEval](https://niceeval.com/) CLI and runtime copy support localization without any configuration in `niceeval.config.ts`. When you need a fixed output language, use an environment variable:

```bash
NICEEVAL_LANG=en npx niceeval list
NICEEVAL_LANG=zh-CN npx niceeval list
```

Detection order is `NICEEVAL_LANG`, `NICEEVAL_LOCALE`, `LC_ALL`, `LC_MESSAGES`, then `LANG`. Values starting with `zh` use `zh-CN`; everything else uses `en`. If none are set, the default is `zh-CN`. This only affects terminal/runtime copy — it does not change the machine-readable fields in result JSON, and it does not translate LLM judge prompts.

## `npx niceeval exp [group|config] [id-prefix...]`

```bash
# Run every experiment under experiments/
npx niceeval exp

# Run one experiment group
npx niceeval exp compare-models

# Run only one configuration in that group (id = group/filename, no extension)
npx niceeval exp compare-models/gpt-5.4

# Inside that group, only run evals whose ID starts with weather
npx niceeval exp compare-models weather
```

<Note>
  Eval-filter arguments only appear after the experiment selector. Bare `npx niceeval weather` does not run; use `npx niceeval exp local weather` or `npx niceeval exp compare weather` instead.
</Note>

The first positional argument accepts either an experiment group name or a single configuration's full id. An exact match on `group/filename` runs only that one configuration in the group — useful for verifying one cell or troubleshooting a single configuration without moving other files out of the directory.

## Common flags

The table below is generated from the CLI's flag-parsing table; any flag outside it fails as an unknown flag with a non-zero exit status. **There is no CLI flag for selecting a sandbox backend**: the sandbox backend can only be set in code — set `sandbox` on the experiment (`defineExperiment`), or in `niceeval.config.ts` (`defineConfig`) as a project-level fallback, using `dockerSandbox()` / `vercelSandbox()` / `e2bSandbox()` from `niceeval/sandbox`.

{/* GENERATED:BEGIN cli-flags */}

{/* This block is generated by pnpm docs:reference from source comments; do not hand-edit. To change the copy, edit the corresponding source TSDoc/JSDoc (mapping in scripts/generate-reference.ts) */}

| Flag | Type | Description |
|---|---|---|
| `--agent` | string | Experiment runs do not support this flag. To switch agents, add or duplicate a config file under `experiments/`. |
| `--model` | string | Experiment runs do not support this flag. To switch models, add or duplicate an experiment file and change `model`. |
| `--runs` | number | How many times to run each eval, commonly used for pass@N. |
| `--max-concurrency` | number | Set how many evals run at the same time. |
| `--timeout` | number | Timeout for a single attempt, in milliseconds. |
| `--budget` | number | Budget limit for the whole run, in dollars. |
| `--tag` | string | Only run evals that carry this tag (see `tags` on `defineEval`). |
| `--junit` | string | Additionally write a JUnit XML report to the given path, for CI consumption. |
| `--json` | string | Additionally write a JSON result (the `RunSummary` serialized as-is) to the given path, for CI or downstream scripts. |
| `--out` | string | `view` command only: statically export the result viewer to the given directory. |
| `--port` | number | `view` command only: set the port the local server listens on. |
| `--transcript` | boolean | `show` command only: render the full conversation and tool calls for a single eval (an evidence facet). |
| `--trace` | boolean | `show` command only: render the text version of the trace waterfall for a single eval (an evidence facet). |
| `--diff` | boolean | `show` command only: a summary of file changes in the sandbox; `--diff=<file-path>` shows the full diff for one file (the path must be joined with `=`). |
| `--history` | boolean | `show` command only: a cross-run timeline that lists only actual executions; mutually exclusive with `--report`. |
| `--experiment` | string | `show` / `view` command only: narrow the selection to a single experiment. |
| `--attempt` | number | `show` command only: pick which attempt to show in the detail / evidence facet (1-indexed, matching the display). |
| `--run` | string | `show` / `view` command only: pin to a specific result directory (a snapshot or `copySnapshots` output). |
| `--report` | string | `show` / `view` command only: replace the default report with your own report file (the file's default export must be `defineReport(...)`). |
| `--dry` | boolean | Only print which evals × run configs would match, without actually running them. |
| `--quiet` | boolean | Turn off the console / live per-result stream and final summary (attempt progress lines still go to stderr); errored / failed results each still print a one-line summary to stderr, while passed / skipped stay silent; the reporter still writes artifacts. |
| `--force` | boolean | Ignore the previous run's results — do not skip (experiment, eval) pairs that already passed, forcing a full rerun. |
| `--strict` | boolean | Recommended in CI: make soft-threshold (`soft`) failures also count toward the whole eval's outcome. |
| `--early-exit` / `--no-early-exit` | boolean | Stop an eval's remaining attempts once one attempt passes. |
| `--open` / `--no-open` | boolean | `view` command only: automatically open a browser after startup (default behavior). |
| `--help` | boolean | Print usage and exit. |
| `--version` | boolean | Print the niceeval version number and exit. |

{/* GENERATED:END cli-flags */}

## `list`

```bash
npx niceeval list
```

Use this to check that eval discovery, IDs, and config loading are working correctly.

## `exp`

```bash
npx niceeval exp compare-models
npx niceeval exp compare-models weather-tool
```

Run a named experiment, comparing agents, models, or flags in a matrix. Arguments from the second position onward filter eval IDs by prefix.

## `view`

```bash
npx niceeval view
```

Open the local result viewer. By default it shows the most recently written results snapshot.

## `--early-exit` and `--strict`

`--early-exit` defaults to `true`: once one attempt of an eval passes, the runner automatically stops the eval's remaining attempts (to save cost). Passing `--early-exit` explicitly therefore has no effect; what's actually useful is turning it off with `--no-early-exit`, which makes `--runs` > 1 run every attempt to completion (to measure the real pass rate instead of stopping early).

`--strict` does not mean "report errors more strictly" — it changes how soft-threshold assertions are judged. A soft threshold like `.atLeast(n)` (`soft` severity) failing normally does not mark the whole eval as `failed` (it just records an assertion that fell short); with `--strict`, a soft threshold falling short also makes the eval's overall outcome count as `failed`. This is recommended in CI, to avoid the case where "the assertion score is short but the eval shows as passed" slips through.

## Environment variable overrides

The following environment variables correspond to some flags, for cases where passing CLI arguments is inconvenient (such as CI environment configuration):

| Environment variable | Corresponding flag |
|---|---|
| `NICEEVAL_RUNS` | `--runs` |
| `NICEEVAL_MAX_CONCURRENCY` | `--max-concurrency` |
| `NICEEVAL_TIMEOUT` | `--timeout` |
| `NICEEVAL_BUDGET` | `--budget` |

Priority from highest to lowest: CLI flag > environment variable > value in the experiment / `niceeval.config.ts` > built-in default. All four environment variables must be valid numbers; parsing failures exit with an error immediately.

[NiceEval](https://niceeval.com/) also automatically loads a `.env` file from the cwd on startup (no extra configuration needed, and it never overrides an environment variable that already exists) — commonly used to keep credentials like `ANTHROPIC_API_KEY` locally without exporting them manually every time.

## Exit codes

| Exit code | Meaning |
|---|---|
| `0` | Every eval passed (or a command like `list`, `init`, `clean` completed normally). |
| `1` | Some eval is `failed` / `errored`; or a command usage error (such as `exp` matching no experiment, or an unsupported flag combination). |
| `2` | A runtime crash (uncaught exception), caught by the CLI's top-level fallback before exiting. |
| `130` | Exit after receiving an interrupt signal (Ctrl-C / SIGTERM). |

If CI needs soft-threshold failures to also count as failures, combine this with `--strict` (see above).
