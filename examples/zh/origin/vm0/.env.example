# 这个示例没有 AGENT_MODE=ai 可用的凭证配置项——vm0(github.com/vm0-ai/vm0)
# 没有可 import 的 npm SDK,也没有公开文档化的 HTTP API 可以填 key 进来接。
# 详见 README.md「调研结论」。AGENT_MODE=ai 在这里只会抛错,不是真集成。
#AGENT_MODE=ai

# 可选:server.ts 监听端口,默认 5588。
#PORT=5588
