# span 发去哪 —— 不设就不导出(span 照样能创建，只是不落地，方便先跑起来看代码结构)。
# 本地起 docker-compose.yml 里的 Jaeger 后用这个:
#OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces

# gen_ai.input.messages / gen_ai.output.messages 是 opt-in —— OTel GenAI semconv 出于隐私
# 考量，默认不采模型输入输出的原文内容。想让 messageIncludes 这类断言有数据可查，才打开:
#OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=true

# 真的调 OpenAI 兼容 API —— 必需。
OPENAI_API_KEY=sk-replace-me
OPENAI_BASE_URL=https://api.openai.com/v1
AGENT_MODEL=gpt-4o-mini

# 可选:server.ts 监听端口，默认 5299。
#PORT=5299
