# 必需:直接传给 Codex SDK 的 `new Codex({ apiKey, baseUrl })`(不是
# OPENAI_API_KEY 转发,SDK 直接把它写进子进程 env.CODEX_API_KEY)。
CODEX_API_KEY=sk-replace-me

# 可选:OpenAI 兼容代理的 base URL(如自建的 s2a 网关)。留空则打官方 OpenAI 端点。
CODEX_BASE_URL=https://your-proxy.example.com/v1

# 可选:传给 ThreadOptions.model,默认 gpt-5.4。
AGENT_MODEL=gpt-5.4

# 可选:server.ts 监听端口,默认 5199(5189 被 examples/zh/origin/claude-agent-sdk 占了)。
#PORT=5199
